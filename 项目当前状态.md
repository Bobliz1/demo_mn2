# MN-BO 项目当前状态报告

**生成时间**: 2026-04-23 23:51  
**报告目的**: 帮助用户快速回顾项目进展和当前状态

---

## 📌 项目概述

**项目名称**: M×N 双层贝叶斯优化（MN-BO）  
**项目路径**: `d:\2602-stu\paper\demo_mn2`  
**研究目标**: 验证 MN-BO 方法在欺骗性函数上的优化效果

**核心思想**:
- **外层**: 在 12 维连续空间搜索最优变换配置（6 个变换算子，每个带强度 gate 和参数）
- **内层**: 对给定变换配置执行标准贝叶斯优化（BO）
- **目的**: 通过目标空间变换改善 BO 在欺骗性函数上的优化性能

---

## 🔬 实验设计（当前配置）

### 默认参数
| 参数 | 符号 | 值 | 说明 |
|------|------|-----|------|
| 重复次数 | R | 5 | seed 42-46，取均值±标准差 |
| 外层初始点 | N_INIT_META | 36 | 拉丁超方采样（12D 空间） |
| 外层迭代 | M_OUTER | 20 | GP 搜索次数 |
| 内层初始点 | n_init | 5 | 内层 BO 初始化采样点数（硬编码） |
| 内层迭代 | N_INNER | 12 | 内层 BO 步数 |
| 探索参数 | XI | 0.05 | EI 采集函数参数 |

### 计算量估算
- 内层每次完整 BO：5（全局初始化）+ 12（BO 迭代步）= 17 次函数评估
- 单次实验（单 seed）：
  - 全局初始化：5 次（一次性，被所有内层 BO 共享复用）
  - 基线 BO：12 次迭代
  - 外层 36 个初始点：36 × 12 = 432 次
  - 外层 20 次搜索：20 × 12 = 240 次
  - **单次实验总计：5 + 12 + 432 + 240 = 689 次函数评估**
- 完整实验（5 次重复）：5 × 689 ≈ **3,445 次函数评估**

---

## 📊 测试函数（共 12 个）

| 函数名 | 理论最大值 | 特点 |
|--------|-----------|------|
| `deceptive_trap_func` | 4.9048 | 欺骗性陷阱函数 |
| `multipeak_func` | 5.0 | 多峰函数 |
| `flat_region_func` | 3.0 | 平坦区域+尖峰 |
| `asymmetric_func` | 10.0 | 非对称函数 |
| `noisy_func` | 4.0 | 高噪声函数 |
| `periodic_trap_func` | 7.0 | 高频周期背景 + 隐藏全局尖峰 |
| `needle_in_haystack_func` | 10.0 | 极窄峰（σ≈0.01）被平坦区包围 |
| `cliff_func` | 8.0 | x=1 处梯度悬崖，GP 难建模 |
| `double_well_func` | 4.0 | 两个等高对称全局峰 |
| `oscillating_decay_func` | 3.5 | 高斯包络×正弦振荡 |
| `step_func` | 5.0 | 阶梯跳变，破坏 GP 平滑性假设 |
| `valley_ridge_func` | 6.0 | V 谷 + 远端山脊 |

---

## 🔧 外层搜索空间（12D 连续参数，v2 带 gate）

| 维度 | 算子 | 参数范围 | 说明 |
|------|------|---------|------|
| p[0] | LogWarper | g ∈ [0,1] | 强度 gate，0=恒等 |
| p[1] | LogWarper | α ∈ [0.1, 10.0] | 对数压缩强度 |
| p[2] | StandardScaler | g ∈ [0,1] | 强度 gate |
| p[3] | StandardScaler | shift ∈ [-1σ, 1σ] | 均值偏移 |
| p[4] | StandardScaler | s ∈ [0.2, 5.0] | 方差缩放 |
| p[5] | PowerTransform | g ∈ [0,1] | 强度 gate |
| p[6] | PowerTransform | p ∈ [-1, 3] | 幂指数 |
| p[7] | SigmoidWarper | g ∈ [0,1] | 强度 gate |
| p[8] | SigmoidWarper | k ∈ [0.01, 10] | Sigmoid 陡度 |
| p[9] | SigmoidWarper | c ∈ [-5σ, 5σ] | Sigmoid 中心 |
| p[10] | MinMaxScaler | g ∈ [0,1] | 强度 gate |
| p[11] | RankTransform | g ∈ [0,1] | 离散 gate，>0.5 启用 |

---

## ✅ 已完成工作（2026-04-22）

### 1. 架构梳理
- 整理了 MN-BO 双层架构全景
- 明确三层结构：文件组织 / 算法逻辑 / 执行流程
- 确认外层 12D 连续空间搜索变换配置

### 2. Bug 修复（src/main.py）
**Bug 1（逻辑错误）**:
- **问题**: `range(N_INIT_META, m_outer)` = `range(36, 20)` = 空循环
- **后果**: 外层 GP 从未执行
- **修复**: 改为 `range(m_outer)`

**Bug 2（性能问题）**:
- **问题**: `n_restarts_optimizer=10` 导致计算量爆炸
- **修复**: 降为 `3`（精度几乎不变，速度快 3×）

### 3. 报告格式重构 + 数值溢出修复
**问题**: 旧汇总报告出现天文数字（gap_diff ~10^200+）

**根本原因**:
- `LogWarper.backward(y_prime)` 当 y_prime~100 时，exp(100)≈2.7×10^43
- `mnbo_gap = gmax - 2.7e43` 变负
- `gap_diff = base_gap - mnbo_gap` 变成天文正数
- 缩减比计算溢出 inf

**修复方案**:
1. **benchmark.py**:
   - 新增 `_fmt(val)` 函数: val<0 → "N/A"，|val|≥1e4 → 科学计数法
   - 新增 `_fmt_gap_diff(val, base_gap)`: gap_diff<0 或 gap_diff>100×base_gap → "N/A"
   - `append_summary_row`: 去除方差/最佳配置列，标题含日期时间，溢出显示 N/A
   - `log_experiment`: 去除方差列，标题含运行时间

2. **main.py** (`run_inner_bo`):
   - 加宽松安全 clamp: `y_max_safe = max(y_init.max()*100, 1e6)`
   - 截断极端 exp 溢出
   - 溢出值在报告层用 N/A 标记，而非在前端隐藏

### 4. Git 初始化
- `git init` + `.gitignore`（排除 results/、__pycache__/、node_modules/）
- 初始 commit: `10f1605`
- 打 tag: `v1.0-bugfix`

---

## ⚠️ 当前未解决问题

### 1. 数值溢出问题（长期修复方向）
**现象**: `double_well_func` 等函数 MN-BO Gap 显示 N/A

**根本原因**:
- `LogWarper.backward(y_prime)` 在 y_prime~30+ 时 exp(y_prime) 达到 10^13
- 远超所有测试函数理论最大值
- 外层 GP 学会「往极端 y_prime 方向搜索」→ MN-BO gap 变负
- 报告正确用 N/A 标记为无效结果

**当前临时方案**:
- 在 `run_inner_bo` 中 clip y_prime 到安全范围（宽松 clamp）
- 报告层用 N/A 标记溢出结果

**长期修复方向**:
- 在 `WrappedTarget` 或 `propose_next_sample` 里 clip y_prime 到安全范围（如 [-50, 50]）
- 或换用有界变换算子

### 2. MEMORY.md 与实际代码不一致
**发现的问题**（已记录到 knowledge）:
- "外层：6维连续空间" → 实际是 12D
- "拉丁超方采样初始化12个点" → 实际是 N_INIT_META=36
- "GP搜索M=8次" → 实际是 M_OUTER=20
- "内层N=15" → 实际是 N_INNER=12

**待办**: 更新 MEMORY.md 和 README.md 中的参数描述

---

## 📝 未提交的修改（Git Status）

### 已修改文件
1. **`.workbuddy/memory/2026-04-22.md`**
   - 工作日志记录

2. **`src/benchmark.py`**
   - 数值溢出修复
   - 报告格式重构

3. **`src/bo_transform.py`**
   - 变换算子实现（连续参数化版本 v2）

4. **`src/main.py`**
   - Bug 修复（空循环、性能优化）
   - 外层搜索逻辑

### 未跟踪文件
- **`question.md`**（当前为空）

**建议**: 实验验证通过后，提交这些修改并打新 tag

---

## 🚀 运行方式

### 交互式菜单（推荐）
```bash
D:\Miniconda3\envs\bo_env\python.exe src/main.py
```

### 命令行
```bash
# 指定函数
D:\Miniconda3\envs\bo_env\python.exe src/main.py cliff_func double_well_func

# 全部函数
D:\Miniconda3\envs\bo_env\python.exe src/main.py --all

# 自定义参数
D:\Miniconda3\envs\bo_env\python.exe src/main.py --all -M 30 -N 15 -R 3
```

---

## 📂 项目文件结构

```
demo_mn2/
├── .workbuddy/              # AI 工作记忆（勿删）
│   └── memory/
│       ├── MEMORY.md        # 项目持久记忆
│       └── 2026-04-22.md   # 昨天的工作日志
├── src/                      # 源码
│   ├── main.py              # 实验入口（交互菜单 / 命令行）
│   ├── benchmark.py         # BO 工具函数 + ExperimentLogger
│   ├── bo_transform.py      # 变换算子（连续参数化，12D 外层空间）
│   ├── generate_ppt.js      # PPT 生成脚本
│   ├── package.json          # npm 依赖
│   └── functions/           # 基准函数包（14 个文件）
│       ├── _base.py         # BenchmarkFunc 抽象基类
│       ├── __init__.py      # REGISTRY 注册表，统一导出
│       ├── deceptive_trap.py / multipeak.py / flat_region.py
│       ├── asymmetric.py / noisy.py / periodic_trap.py
│       ├── needle_in_haystack.py / cliff.py / double_well.py
│       ├── oscillating_decay.py / step.py / valley_ridge.py
├── results/                  # 实验输出
│   ├── summary_report.md    # 汇总对比表
│   ├── archive/             # 历史图片归档
│   └── <func_name>/         # 按函数分目录
│       ├── <func_name>_<ts>.md   # 单函数报告
│       └── plots/plot_<ts>.png    # 可视化图
├── reports/                  # 文档与汇报材料
│   ├── 思路.md
│   ├── 当前情况与架构.md
│   ├── 下一步安排.md
│   ├── agent_architecture.html
│   └── MN-BO组会报告.pptx
├── README.md
├── question.md               # 当前为空
└── node_modules/            # npm 依赖
```

---

## 🎯 下一步建议

### 短期（本次会话）
1. **验证修复效果**: 运行单个函数测试（如 `cliff_func`），确认数值溢出问题是否已妥善处理
2. **完整实验**: 如果单函数测试通过，运行全部 12 个函数
3. **提交代码**: 实验成功后，提交修改并打 tag `v1.1-overflow-fix`

### 中期
1. **更新文档**: 修正 MEMORY.md 和 README.md 中的过时参数描述
2. **长期修复溢出**: 在 `WrappedTarget` 中实现更严格的 y_prime 安全截断
3. **结果分析**: 分析 12 个函数的实验结果，总结 MN-BO 在各类欺骗性函数上的表现

### 长期
1. **算法改进**: 根据实验结果调整外层搜索策略
2. **论文撰写**: 整理实验数据，开始撰写论文
3. **代码优化**: 进一步提升计算效率（并行化、GPU 加速等）

---

## 📖 关键代码文件说明

### src/main.py
- **作用**: 实验入口，处理命令行参数，协调外层/内层 BO
- **关键函数**:
  - `run_inner_bo()`: 执行内层 BO
  - `_single_run()`: 单次实验（一次 seed）
  - `run_experiment()`: 多次重复实验（取均值）
  - `interactive_menu()`: 交互式菜单

### src/benchmark.py
- **作用**: BO 工具层，提供采集函数、采样点建议、实验日志
- **关键函数**:
  - `expected_improvement()`: EI 采集函数
  - `propose_next_sample()`: 建议下一采样点（L-BFGS-B 优化）
  - `ExperimentLogger`: 实验日志记录类

### src/bo_transform.py
- **作用**: 目标空间变换算子库（连续参数化版本 v2）
- **关键类**:
  - `LogWarper`: 对数变换
  - `StandardScaler`: 标准化
  - `PowerTransform`: 幂变换
  - `SigmoidWarper`: Sigmoid 压缩
  - `MinMaxScaler`: 最小-最大缩放
  - `RankTransform`: 秩变换（离散）
  - `TransformerPipeline`: 算子流水线
  - `WrappedTarget`: 包装后的目标函数
- **关键函数**:
  - `params_to_pipeline()`: 参数向量 → 算子流水线
  - `get_outer_bounds()`: 返回外层搜索空间边界

---

## 💡 补充说明

### 为什么需要目标空间变换？
标准 BO 使用高斯过程（GP）作为替代模型，GP 假设函数平滑、连续。但欺骗性函数往往具有：
- 不连续梯度（如 `cliff_func`）
- 平坦区域（`flat_region_func`）
- 多峰（`multipeak_func`）
- 极端尖峰（`needle_in_haystack_func`）

这些特性违反 GP 的平滑性假设，导致 BO 性能下降。通过目标空间变换，我们可以：
1. 压缩异常值
2. 使函数形态更"GP-friendly"
3. 提升 BO 的优化效果

### 外层 GP 学习什么？
外层 GP 的输入是 12 维变换配置参数，输出是 Gap（理论最大值与找到的最大值之差）。
外层 GP 的目标是：**学习「哪种变换配置在哪些函数上效果最好」**，从而指导搜索最优变换配置。

---

**报告结束** | 如有疑问，请查看 `reports/` 目录下的详细文档或询问 AI 助手
