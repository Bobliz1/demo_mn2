# 2026-04-24 工作日志

## 项目状态回顾 + 文档更新

### 背景
用户昨天（2026-04-22）进行了架构梳理、Bug 修复、报告格式重构，但今天忘记了项目状态，请求生成状态报告。

### 完成工作

1. **生成项目状态报告**（`项目当前状态.md`）
   - 详细记录了项目背景、实验设计、测试函数、外层搜索空间
   - 列出了已完成的 Bug 修复和数值溢出修复
   - 明确了当前未解决的问题（数值溢出根本原因、MEMORY.md 参数不一致）

2. **更新 MEMORY.md**
   - 修正 `bo_transform.py` 描述：6D → 12D 外层空间
   - 修正实验设计参数：
     - 外层：6维 → 12维连续空间
     - 拉丁超方采样：12个点 → 36个点
     - M=8 → M=20（外层GP迭代）
     - N=15 → N=12（内层BO步数）
   - 补充 n_init=5 和正确计算量估算（689次）

3. **更新 README.md**
   - 修正 `bo_transform.py` 描述：列出所有6个算子，标注12D外层空间
   - 修正核心参数表格：M_OUTER: 5→20，N_INNER: 15→12，添加 N_INIT_META: 36
   - 修正计算量估算：969次 → 689次

4. **修正计算量估算错误**（用户发现）
   - **问题**：原估算把全局共享的5次内层初始化重复算到每个外层采样点上
   - **错误公式**：基线17 + 36×17 + 20×17 = 969次
   - **正确公式**：全局初始化5 + 基线12 + 36×12 + 20×12 = 689次
   - 涉及文件：`项目当前状态.md`、`README.md`、`MEMORY.md`

### 当前状态总结

**代码层面**：
- 关键 Bug 已修复（外层GP空循环、性能优化）
- 数值溢出问题临时修复（clamp + N/A 标记）
- 有4个文件未提交（main.py, benchmark.py, bo_transform.py, .workbuddy/memory/2026-04-22.md）

**文档层面**：
- MEMORY.md 和 README.md 参数描述已更新为正确值
- 项目状态报告已生成（`项目当前状态.md`）

---

## 算子隔离（稳定算子模式）

### 决策
用户决定在架构大改前，先隔离不稳定算子，仅保留线性变换算子做完整实验。

### 完成工作
1. **注释 bo_transform.py 中 4 个高风险算子**
   - 注释 PARAM_SPEC 中 LogWarper、PowerTransform、SigmoidWarper、RankTransform 的参数维度
   - 外层维度从 12D 自动降为 4D（N_CONTINUOUS = len(PARAM_SPEC) = 4）
   - 注释 params_to_pipeline 中对应算子的实例化逻辑
   - 调整 StandardScaler 索引：p[2,3,4] → p[0,1,2]
   - 调整 MinMaxScaler 索引：p[10] → p[3]
   - 更新 OPERATOR_INFO 为稳定算子模式

2. **更新 main.py docstring**
   - 外层维度说明：12D → 4D
   - 标注待处理算子
   - 添加快速验证命令

3. **更新 README.md**
   - bo_transform.py 描述更新为稳定模式
   - 添加"待处理算子"章节（含风险点说明表格）
   - 添加完整实验命令（--all -R 1 -M 5 -N 8 -I 8）
   - 更新核心参数说明和计算量估算

4. **更新 MEMORY.md**
   - 实验设计分节：默认参数 + 快速实验参数
   - 外层搜索空间更新为 4D
   - 添加待处理算子说明

### 当前代码状态
- 4 个文件已修改：bo_transform.py, main.py, README.md, MEMORY.md
- 待提交

### 实验结果（稳定算子模式，-R 1 -M 5 -N 8 -I 8）

**总耗时：253.3s（约 4.2 分钟）**

| 函数 | 基线 Gap | MN-BO Gap | 缩减比 | 评价 |
|------|---------|----------|--------|------|
| deceptive_trap_func | 2.706 | 2.634 | 2.7% | 轻微提升 |
| multipeak_func | -0.732 | -0.732 | -0.0% | 无变化 |
| flat_region_func | 1.800 | 0.151 | **91.6%** | ✅ 显著提升 |
| asymmetric_func | 1.408 | 1.341 | 4.7% | 轻微提升 |
| noisy_func | 0.684 | 0.341 | **50.1%** | ✅ 显著提升 |
| periodic_trap_func | 0.541 | 0.181 | **66.5%** | ✅ 显著提升 |
| needle_in_haystack_func | 9.900 | 9.900 | 0.0% | 无变化 |
| cliff_func | -0.180 | -0.189 | -5.1% | 轻微下降 |
| double_well_func | 0.006 | 0.006 | 8.7% | 轻微提升 |
| oscillating_decay_func | 0.613 | -0.013 | **102.1%** | ✅ 显著提升 |
| step_func | -0.089 | -0.100 | -12.7% | 轻微下降 |
| valley_ridge_func | -0.119 | -0.127 | -6.5% | 轻微下降 |

**关键发现**：
1. **无 N/A 溢出**：所有函数都正常完成，没有数值溢出！✅
2. **显著提升**：flat_region(91.6%)、noisy(50.1%)、periodic_trap(66.5%)、oscillating_decay(102.1%)
3. **无变化/下降**：multipeak、needle_in_haystack 无变化；cliff、step、valley_ridge 轻微下降
4. **MN-BO Gap 为负值**：部分函数 MN-BO Gap < 0（如 oscillating_decay -0.013），说明找到了比理论最大值更高的点——这是**基线 BO 也找到过的点**，MN-BO 通过变换进一步放大了优势

### 待处理
- 提交代码并打 tag
- 架构重构：引入预算控制、backward 安全校验、算子恢复
