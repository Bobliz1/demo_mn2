# 2026-04-22 工作日志

## 架构梳理 + Bug 修复 + Git 初始化

### 架构梳理
- 整理了 MN-BO 双层架构全景（见本次对话）
- 三层结构：文件组织 / 算法逻辑 / 执行流程
- 外层 12D 连续空间搜索变换配置；内层标准 BO

### Bug 修复（src/main.py）
1. **Bug1（逻辑错误）**：`range(N_INIT_META, m_outer)` 改为 `range(m_outer)`
   - 原代码 `range(36, 20)` = 空循环，外层 GP 从未执行
2. **Bug2（性能）**：`n_restarts_optimizer=10` 降为 `3`
   - 10 次重启在 36×12×5 次 GP 循环里是计算量爆炸的主因

### Git 初始化
- `git init` + `.gitignore`（排除 results/、__pycache__、node_modules）
- 初始 commit：`10f1605`
- 打 tag：`v1.0-bugfix`
- 以后每次实验前建议打 tag：`git tag -a v1.x-<描述> HEAD -m "..."`
