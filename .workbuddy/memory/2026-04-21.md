# 2026-04-21 日志

## MN-BO 项目文档整理

- 阅读 `思路.md` 和 `agent_architecture.html`，结合代码完整理解架构
- 输出两份 md 文档到 `docs/` 目录：
  - `当前情况与架构.md`：双层 BO 架构、文件结构、12 个测试函数、当前实验结果
  - `下一步安排.md`：四个方向（可视化增强、变换算子扩充、实验完整性、理论扩展）+ 近期行动计划
- 优先方向：可视化（收敛曲线、热力图）+ 完成全部 12 函数实验 + 扩充变换算子

## 工作空间整理

- 根目录重构：`src/`（源码）、`results/`（实验输出）、`reports/`（文档/汇报）、`.workbuddy/`（AI记忆）
- 源码迁移：`main.py / benchmark.py / bo_transform.py / generate_ppt.js / functions/` → `src/`
- 文档迁移：`思路.md / agent_architecture.html / MN-BO组会报告.pptx / docs/*.md` → `reports/`
- 历史图片归档：`plots/` → `results/archive/`
- 迁移过程中 `functions/` 14个Python文件意外删除，已从 `d:\2602-stu\paper\demo_mn` 备份恢复
- 新增 `README.md`
- ⚠️ 运行命令改为：`D:\Miniconda3\envs\bo_env\python.exe src/main.py ...`
